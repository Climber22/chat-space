.group-bar
  .group-bar__text
    %h1
      = groups.find(params[:group_id]).name
    %p Menbars: #{groups.find(params[:group_id]).users.pluck(:name).join(" ")}
  = link_to "Edit", edit_group_path(id: params[:group_id]), class: "group-bar__edit"
